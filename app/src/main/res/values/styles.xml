<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Activities theme -->
    <style name="ActivityTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
<!--        <item name="android:windowContentOverlay">@null</item>-->
    </style>

    <!-- Preference Activity theme -->
    <style name="PrefsTheme" parent="ActivityTheme">
        <item name="android:windowBackground">@color/light_gray</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:selectableItemBackground">@color/grey</item>
    </style>

    <style name="DividerHorizontal">
        <item name="android:background">@color/mBlack</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
    </style>

    <!-- Dialog Theme -->
    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:background">@color/light_gray</item>
    </style>
</resources>
